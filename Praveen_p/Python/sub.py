#!/usr/bin/python
import subprocess
from subprocess import *
#subprocess.call(["ls", "-l"])
#subprocess.call(["df", "-h"])
#subprocess.call(["echo $HOME"])
#subprocess.call(["pwd"])
#call(["java", "-version"])
#check_output("ls", shell=True)
#check_output(["ls", "sys.py"])
